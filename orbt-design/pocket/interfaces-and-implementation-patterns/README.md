# Interfaces & implementation patterns

The core Orbt UCE contract already supports pockets via `pockets[asset]`, per-allocator pocket mapping, and UCE-side **allowance pulls**. Below are thin adapters you’ll typically pair with a pocket.
